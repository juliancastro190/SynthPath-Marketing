# Lean dependency set for hosting the Discord bridge (griffin/discord_bridge.py,
# discord_main.py), which also runs the heartbeat in the background — see
# DEPLOY.md. Same reasoning as requirements-heartbeat.txt: Tier 3's voice
# packages (deepgram-sdk, sounddevice, pynput) need real audio hardware/a
# display and aren't reachable from this entry point's import chain at
# all (verified via static import-graph analysis), so they're left out to
# avoid risking the build on a headless server for no benefit.
#
# For local, full-featured use (voice included), use requirements.txt
# instead — this file is deploy-only.

anthropic>=0.40.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
elevenlabs>=1.0.0
discord.py>=2.3.0
